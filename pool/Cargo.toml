[package]
name = "pool"
version = "0.1.0"
edition = "2024"

[dependencies]
crossbeam = { version = "0.8.4", features = ["crossbeam-channel"] }
parking_lot = "0.12.4"

[[bench]]
name = "pool"
harness = false

[dev-dependencies]
criterion = "0.6.0"
num-complex = "0.4.6"
