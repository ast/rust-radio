// Copyright SM6WJM 2026

/// WebRTC data channel for rig control messages (frequency, mode, PTT, spectrum).
pub struct DataChannel {
    // TODO: webrtc data_channel::RTCDataChannel
}

impl DataChannel {
    pub fn new() -> Self {
        Self {}
    }
}
