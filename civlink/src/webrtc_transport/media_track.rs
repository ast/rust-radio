// Copyright SM6WJM 2026

/// Manages WebRTC audio tracks for streaming radio audio to/from the browser.
pub struct MediaTrack {
    // TODO: webrtc track_local / track_remote
}

impl MediaTrack {
    pub fn new() -> Self {
        Self {}
    }
}
